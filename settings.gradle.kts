pluginManagement {
    repositories {
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "kmm-ktor-starter"
include(":shared")
include(":androidApp")
include(":backend")
include(":iosApp")
